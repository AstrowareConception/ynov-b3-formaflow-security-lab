# TLS local

Les fichiers générés sont placés dans `infra/tls/generated/`, ignoré par Git. La génération, la terminaison HTTPS, la redirection HTTP et leurs limites sont ajoutées au checkpoint de remédiation.
